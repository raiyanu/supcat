[workspace]
resolver = "2"

members = [
    "crates/cli",
    "crates/common",
    "crates/core",
    "crates/output",
    "crates/tui",
]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
